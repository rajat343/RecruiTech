# CrewAI (GitHub agent, LeetCode agent, consolidation agent)
crewai==1.9.3

# OpenAI SDK (ATS scorer + CrewAI backend)
openai>=1.0.0

# HTTP client (GitHub/LeetCode GraphQL tools)
httpx==0.28.1

# AWS S3 resume loading
boto3>=1.34.0
pypdf>=4.0.0

# Kafka (persist_and_notify task)
kafka-python-ng==2.2.3

# MongoDB (persisting evaluation results)
pymongo>=4.6.0

# Schema validation
pydantic>=2.0.0

# CrewAI transitive dependency
python-dotenv>=1.0.0
